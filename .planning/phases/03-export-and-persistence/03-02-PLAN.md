---
phase: 03-export-and-persistence
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/pdf/styles.ts
  - src/pdf/buildReportData.ts
  - src/pdf/ReportHeader.tsx
  - src/pdf/EventSection.tsx
  - src/pdf/TotalsSection.tsx
  - src/pdf/IRSNoticeSection.tsx
  - src/pdf/DonationReport.tsx
  - src/components/ExportButton/ExportButton.tsx
  - src/components/ExportButton/ExportContent.tsx
  - src/App.tsx
autonomous: false
requirements: [EXPO-01, EXPO-02, EXPO-03, EXPO-04]

must_haves:
  truths:
    - "User can click Export PDF and receive a downloadable PDF file"
    - "PDF contains all donation events with dates, organization names, itemized quantities, FMV values, and grand total"
    - "PDF includes Form 8283 field reference callouts when total exceeds $500 or item exceeds $5,000"
    - "PDF includes a legal disclaimer on every page"
    - "PDF renders blank donor info fields (not collected in v1 per D-14)"
    - "Poor-condition items under $500 appear dimmed with a non-deductible note and are excluded from totals"
    - "@react-pdf/renderer is lazy-loaded — not in the main bundle"
  artifacts:
    - path: "src/pdf/DonationReport.tsx"
      provides: "Root PDF Document component receiving ReportData snapshot"
      exports: ["DonationReport"]
    - path: "src/pdf/buildReportData.ts"
      provides: "Pure function: DonationStore -> ReportData snapshot"
      exports: ["buildReportData", "ReportData"]
    - path: "src/pdf/EventSection.tsx"
      provides: "Per-event table with item rows, condition, qty, FMV, line total"
      exports: ["EventSection"]
    - path: "src/pdf/IRSNoticeSection.tsx"
      provides: "Form 8283 callout box + legal disclaimer"
      exports: ["IRSNoticeSection"]
    - path: "src/components/ExportButton/ExportButton.tsx"
      provides: "Lazy-loading PDF export trigger"
      exports: ["ExportButton"]
    - path: "src/App.tsx"
      provides: "Wires ExportButton, ExportJSONButton, and ImportBackupButton into the UI"
  key_links:
    - from: "src/components/ExportButton/ExportButton.tsx"
      to: "src/components/ExportButton/ExportContent.tsx"
      via: "React.lazy(() => import('./ExportContent'))"
      pattern: "React\\.lazy"
    - from: "src/components/ExportButton/ExportContent.tsx"
      to: "src/pdf/DonationReport.tsx"
      via: "usePDF hook renders DonationReport"
      pattern: "usePDF"
    - from: "src/pdf/DonationReport.tsx"
      to: "src/pdf/buildReportData.ts"
      via: "ReportData prop type"
      pattern: "ReportData"
    - from: "src/pdf/EventSection.tsx"
      to: "src/store/selectors.ts"
      via: "isDeductible check per item row"
      pattern: "isDeductible"
    - from: "src/App.tsx"
      to: "src/components/ExportButton/ExportButton.tsx"
      via: "import and render in toolbar area"
      pattern: "ExportButton"
---

<objective>
Build PDF export with Form 8283 annotations and wire all Phase 3 UI controls (PDF export, JSON backup, JSON restore) into the app.

Purpose: Users need a downloadable PDF suitable for CPA handoff containing all donation data, IRS compliance callouts, and a legal disclaimer. This plan also wires the backup buttons from Plan 01 into the UI.

Output: Complete PDF component tree, lazy-loaded ExportButton, and App.tsx updated with all Phase 3 controls.
</objective>

<execution_context>
@/Users/tylermeans/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tylermeans/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-export-and-persistence/03-RESEARCH.md
@.planning/phases/03-export-and-persistence/03-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from prior phases and Plan 01 -->

From src/store/types.ts:
```typescript
export interface DonationItem {
  id: string; catalogSlug: string; name: string; category: string
  quantity: number; condition: ConditionType
  fmv_low: number; fmv_mid: number; fmv_high: number; fmv_selected: number
  irsNote?: string
}
export interface DonationEvent {
  id: string; date: string; organization: string; items: DonationItem[]
}
export interface PersistedState {
  schemaVersion: 1; taxYear: number; events: DonationEvent[]
}
```

From src/store/index.ts (after Plan 01):
```typescript
export interface DonationStore extends PersistedState {
  setTaxYear: (year: number) => void
  addEvent: (...) => void
  removeEvent: (eventId: string) => void
  updateEvent: (...) => void
  addItem: (...) => void
  updateItem: (...) => void
  removeItem: (...) => void
  replaceAll: (state: Pick<PersistedState, 'taxYear' | 'events'>) => void
}
```

From src/store/selectors.ts:
```typescript
export const isDeductible = (item: DonationItem): boolean =>
  item.condition !== 'poor' || item.fmv_selected > 500
export const selectGrandTotal = (state: DonationStore): number => ...
export const selectTotalsByCategory = (state: DonationStore): Record<string, number> => ...
export const selectAggregateThresholdFlags = (state: DonationStore): {
  requiresForm8283SectionA: boolean
  requiresQualifiedAppraisal: boolean
  highValueItemName?: string
} => ...
```

From Plan 01 (components ready to wire):
```typescript
// src/components/ExportJSONButton.tsx
export function ExportJSONButton(): JSX.Element
// src/components/ImportBackupButton.tsx
export function ImportBackupButton(): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @react-pdf/renderer + build PDF component tree</name>
  <files>src/pdf/styles.ts, src/pdf/buildReportData.ts, src/pdf/ReportHeader.tsx, src/pdf/EventSection.tsx, src/pdf/TotalsSection.tsx, src/pdf/IRSNoticeSection.tsx, src/pdf/DonationReport.tsx</files>
  <read_first>src/store/types.ts, src/store/selectors.ts, src/store/index.ts, .planning/phases/03-export-and-persistence/03-RESEARCH.md (Pattern 2, Pattern 3, Pattern 4 sections, and Code Examples section)</read_first>
  <action>
0. Install the dependency:
   ```bash
   cd /Users/tylermeans/github/tax-donations-writeoffs-app && npm install @react-pdf/renderer
   ```

1. Create `src/pdf/styles.ts` — shared StyleSheet constants:
   - Import `StyleSheet` from `@react-pdf/renderer`
   - Use ONLY built-in fonts: `Helvetica`, `Helvetica-Bold`, `Helvetica-Oblique` — NO `Font.register()` calls (known v4 bug causes usePDF to hang)
   - Define and export: `pageStyles` (fontFamily: 'Helvetica', fontSize: 10, padding: 40/60/40), `footerStyles` (position: absolute, bottom: 30, fontSize: 8, color: #666, border-top), `tableStyles` (header row, data row, strikethrough row, column widths: name 40%, condition 15%, qty 10%, fmv 15%, total 20%), `sectionStyles` (marginBottom: 16, header bold 11pt, subheader 9pt #555), `noticeBoxStyles` (border, background #f8f8f8, padding: 10)
   - Export a `formatCurrency(n: number): string` helper returning `$X.XX`
   - Export a `formatDate(iso: string): string` helper using `new Date(\`${iso}T00:00:00\`)` to prevent timezone off-by-one (established Phase 2 pattern)

2. Create `src/pdf/buildReportData.ts`:
   - Import selectors: `selectGrandTotal`, `selectTotalsByCategory`, `selectAggregateThresholdFlags` from `../store/selectors`
   - Import type `DonationStore` from `../store`
   - Define and export `ReportData` interface: `{ taxYear: number, events: DonationStore['events'], grandTotal: number, totalsByCategory: Record<string, number>, thresholds: ReturnType<typeof selectAggregateThresholdFlags>, generatedAt: string }`
   - Export `buildReportData(state: DonationStore): ReportData` — a pure function that snapshots the store state into ReportData. `generatedAt` = `new Date().toISOString()`

3. Create `src/pdf/ReportHeader.tsx`:
   - Props: `{ taxYear: number, generatedAt: string }`
   - Content (using @react-pdf/renderer View/Text):
     - Title: "Donation Summary — Tax Year {taxYear}" (Helvetica-Bold, 14pt)
     - Subtitle: "Non-Cash Charitable Contributions" (10pt, #555)
     - Donor fields as BLANK lines per D-14: "Taxpayer Name: _______________________" and "Taxpayer ID (SSN/EIN): _______________________" — these are NOT input fields, just printed text with blanks for the user to fill in on paper
     - Generated date: "Generated: {formatted date}" (8pt, right-aligned)

4. Create `src/pdf/EventSection.tsx`:
   - Props: `{ event: DonationEvent }`
   - Import `isDeductible` from `../store/selectors`
   - Render: event organization name (bold), donation date (formatted), then a table with columns: Item, Condition, Qty, FMV Each, Line Total
   - For each item in event.items:
     - If `isDeductible(item)` is true: normal row, Line Total = `fmv_selected * quantity`
     - If `isDeductible(item)` is false: dimmed row (opacity: 0.4), Line Total shows "---", add note below item name: "Not deductible -- IRC S170(f)(16)" (fontSize: 7, color: #c00)
   - Use `wrap={false}` only on the section header View (not the entire section), so item rows can wrap across pages. Add `minPresenceAhead={40}` on the section header to prevent orphaned headers.

5. Create `src/pdf/TotalsSection.tsx`:
   - Props: `{ grandTotal: number, totalsByCategory: Record<string, number> }`
   - Render: "Deduction Summary" header (bold), then a list of category: $amount rows, then a bold "Grand Total: $X.XX" row with a top border line

6. Create `src/pdf/IRSNoticeSection.tsx`:
   - Props: `{ thresholds: { requiresForm8283SectionA: boolean, requiresQualifiedAppraisal: boolean, highValueItemName?: string } }`
   - Render a bordered box with:
     - Title: "IRS Filing Notes" (bold)
     - If `requiresForm8283SectionA`: bullet about Form 8283 Section A with column references (a)-(i), noting FMV method = "Thrift store value -- Salvation Army Valuation Guide 2025"
     - If `requiresQualifiedAppraisal`: bullet about Section B + qualified appraisal for the named item
     - If neither: note that no additional forms are required, but mention $250 per-event written acknowledgment
     - Legal disclaimer (Helvetica-Oblique, 8pt): "DISCLAIMER: This report provides estimates based on Salvation Army and Goodwill valuation guides (2025). These are not IRS-authorized values. Fair market value is determined by what a buyer would pay for the item in its current condition. Consult a qualified tax professional before filing. Donation Itemizer is not responsible for the accuracy of your deductions."

7. Create `src/pdf/DonationReport.tsx`:
   - Import `Document`, `Page`, `View`, `Text` from `@react-pdf/renderer`
   - Import all sub-components: ReportHeader, EventSection, TotalsSection, IRSNoticeSection
   - Import ReportData type from `./buildReportData`
   - Import styles from `./styles`
   - Props: `{ data: ReportData }`
   - Render: `<Document title="Donation Summary {taxYear}"><Page size="LETTER" style={pageStyles}>` containing ReportHeader, then map `data.events` to EventSection components, then TotalsSection, then IRSNoticeSection, then a fixed footer View with the disclaimer text + generation date
   - The fixed footer uses `fixed` prop on View to repeat on every page

CRITICAL CONSTRAINTS:
- Use ONLY Helvetica built-in fonts. Do NOT call Font.register() — it causes usePDF to hang in v4.
- All @react-pdf/renderer imports must be in these src/pdf/ files and in ExportContent.tsx (Task 2). NEVER import @react-pdf/renderer in App.tsx or any eagerly-loaded component.
- Use `@react-pdf/renderer` View/Text/StyleSheet — NOT HTML elements (div/span/p). These are PDF primitives, not DOM elements.
  </action>
  <verify>
    <automated>cd /Users/tylermeans/github/tax-donations-writeoffs-app && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains @react-pdf/renderer in dependencies
    - All 7 src/pdf/ files exist: styles.ts, buildReportData.ts, ReportHeader.tsx, EventSection.tsx, TotalsSection.tsx, IRSNoticeSection.tsx, DonationReport.tsx
    - grep confirms: `fontFamily: 'Helvetica'` in styles.ts (built-in font only)
    - grep confirms: NO `Font.register` in any src/pdf/ file
    - grep confirms: `isDeductible` imported in EventSection.tsx
    - grep confirms: `opacity: 0.4` or equivalent dimming for non-deductible rows in EventSection.tsx
    - grep confirms: "Taxpayer Name" blank line in ReportHeader.tsx (D-14 compliance)
    - grep confirms: "DISCLAIMER" text in IRSNoticeSection.tsx (EXPO-04)
    - grep confirms: "Form 8283" in IRSNoticeSection.tsx (EXPO-03)
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <done>Complete PDF component tree renders donation data with Form 8283 callouts, legal disclaimer, blank donor fields, and proper poor-condition handling — all using built-in Helvetica fonts only</done>
</task>

<task type="auto">
  <name>Task 2: ExportButton with lazy-load + wire all Phase 3 controls into App.tsx</name>
  <files>src/components/ExportButton/ExportButton.tsx, src/components/ExportButton/ExportContent.tsx, src/App.tsx</files>
  <read_first>src/App.tsx, src/pdf/DonationReport.tsx (created in Task 1), src/pdf/buildReportData.ts (created in Task 1), src/components/ExportJSONButton.tsx (from Plan 01), src/components/ImportBackupButton.tsx (from Plan 01), .planning/phases/03-export-and-persistence/03-RESEARCH.md (Pattern 1 section)</read_first>
  <action>
1. Create directory `src/components/ExportButton/`

2. Create `src/components/ExportButton/ExportContent.tsx`:
   - This is the lazy boundary — @react-pdf/renderer only loads when this module is imported
   - Import `usePDF` from `@react-pdf/renderer`
   - Import `DonationReport` from `../../pdf/DonationReport`
   - Import `buildReportData` from `../../pdf/buildReportData`
   - Import `useDonationStore` from `../../store`
   - Props: `{ onDone: () => void }`
   - In component body: read full store state via `useDonationStore()`, call `buildReportData(state)` to get snapshot
   - Call `usePDF({ document: <DonationReport data={reportData} /> })`
   - Render states:
     - `instance.loading`: show "Generating PDF..." text with a spinner or pulsing animation (prefers-reduced-motion safe)
     - `instance.error`: show "PDF generation failed. Please try again." with a retry button
     - Ready: render an `<a>` tag with `href={instance.url}`, `download={donations-${reportData.taxYear}.pdf}`, styled as a primary button. On click call `onDone()`.
   - Export as default (required for React.lazy)

3. Create `src/components/ExportButton/ExportButton.tsx`:
   - Import `React`, `Suspense`, `useState` from `react`
   - Import `FileDown` icon from `lucide-react`
   - Use `React.lazy(() => import('./ExportContent'))` for the lazy boundary
   - Component renders a button "Export PDF" with FileDown icon, `cursor-pointer`, `aria-label="Export donation summary as PDF"`
   - On click: set `showPDF` state to true
   - When `showPDF` is true: render `<Suspense fallback={<span>Preparing PDF...</span>}><ExportContent onDone={() => setShowPDF(false)} /></Suspense>`
   - Export as named export `ExportButton`

4. Update `src/App.tsx` to wire all Phase 3 controls:
   - Add imports for: `ExportButton` from `./components/ExportButton/ExportButton`, `ExportJSONButton` from `./components/ExportJSONButton`, `ImportBackupButton` from `./components/ImportBackupButton`
   - Add a toolbar section between ThresholdFlags and DonationEventList/EmptyState, wrapped in a `<div>` with flex layout:
     - Left side: `<ExportButton />` (primary action)
     - Right side: `<ExportJSONButton />` and `<ImportBackupButton />` (secondary actions)
   - The toolbar should only render when `hasEvents` is true (no point exporting empty data)
   - The ImportBackupButton should ALWAYS render (even without events — user may want to restore)
   - Wrap the toolbar in a responsive layout: on mobile (default) stack vertically with gap-2, on md+ use flex-row with justify-between
   - Maintain the existing component order: TotalsDashboard, ThresholdFlags, [new toolbar], DonationEventList/EmptyState

CRITICAL: Do NOT import @react-pdf/renderer in ExportButton.tsx or App.tsx. Only ExportContent.tsx (the lazy boundary) imports it.
  </action>
  <verify>
    <automated>cd /Users/tylermeans/github/tax-donations-writeoffs-app && npx tsc --noEmit 2>&1 | head -30 && npx vite build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ExportButton/ExportButton.tsx exists with React.lazy import
    - src/components/ExportButton/ExportContent.tsx exists as default export with usePDF usage
    - grep confirms: `React.lazy` in ExportButton.tsx
    - grep confirms: `@react-pdf/renderer` imported ONLY in ExportContent.tsx and src/pdf/ files (NOT in ExportButton.tsx, NOT in App.tsx)
    - grep confirms: `cursor-pointer` on ExportButton
    - grep confirms: `aria-label` on ExportButton
    - App.tsx imports ExportButton, ExportJSONButton, and ImportBackupButton
    - App.tsx renders ImportBackupButton outside the `hasEvents` guard (always visible)
    - `npx tsc --noEmit` passes with zero errors
    - `npx vite build` succeeds and @react-pdf/renderer appears in a separate chunk (not main bundle)
  </acceptance_criteria>
  <done>Export PDF button lazy-loads the PDF generator; all three Phase 3 buttons (Export PDF, Save Backup, Restore Backup) are wired into App.tsx; the app builds successfully with @react-pdf/renderer code-split into its own chunk</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify PDF export and JSON backup flows</name>
  <what-built>
    Complete Phase 3: PDF export with Form 8283 annotations and legal disclaimer, JSON backup export/import with Zod validation, localStorage persistence hardening with schema guard.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. Open http://localhost:5173 in browser

    **Test PDF Export (EXPO-01 through EXPO-04):**
    3. Create a donation event with date and organization name
    4. Add 2-3 items in different conditions (good, excellent, and one poor-condition item)
    5. Click "Export PDF" button
    6. Verify the PDF downloads with filename `donations-2025.pdf`
    7. Open the PDF and verify:
       - Title says "Donation Summary -- Tax Year 2025"
       - Blank donor name/ID lines are present (not input fields)
       - Each donation event shows organization, date, and item table
       - Poor-condition items appear dimmed with "Not deductible" note
       - Grand total matches the dashboard total (poor items excluded)
       - Category breakdown is listed
       - IRS Filing Notes box appears (if total > $500, mentions Form 8283)
       - Legal disclaimer text appears at bottom of every page

    **Test JSON Backup (PERS-03 and PERS-04):**
    8. Click "Save Backup" button -- verify a .json file downloads
    9. Open the JSON file -- verify it contains events, taxYear, schemaVersion
    10. Delete all events from the app (or clear localStorage)
    11. Click "Restore Backup" and select the saved .json file
    12. Verify all events and items are restored exactly as before
    13. Try importing a random non-JSON file -- verify friendly error message appears

    **Test Persistence (PERS-01 and PERS-02):**
    14. With data loaded, close and reopen the browser tab
    15. Verify all data is restored exactly as left

    Expected: All flows work end-to-end with no console errors.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npx vite build` succeeds — check output for separate chunk containing @react-pdf/renderer
- PDF opens in any PDF reader with selectable text (not rasterized image)
- JSON backup roundtrips: export then import produces identical store state
- localStorage survives browser tab close/reopen
- Corrupt localStorage triggers console.warn and clean reset (not crash)
</verification>

<success_criteria>
- User clicks "Export PDF" and receives a formatted, downloadable PDF
- PDF contains donor placeholder fields, event tables, totals, Form 8283 callouts, and legal disclaimer
- Poor-condition items are visually distinct and excluded from PDF totals
- @react-pdf/renderer is NOT in the main bundle (lazy-loaded)
- JSON export downloads a valid .json file with all donation data
- JSON import restores data after Zod validation
- All three buttons (Export PDF, Save Backup, Restore Backup) are accessible and visible in the app
- App builds with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-export-and-persistence/03-02-SUMMARY.md`
</output>
